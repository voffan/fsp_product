import {
  // Dispatch,
  ReactNode,
  //  SetStateAction
} from "react"

export interface INotificationWindowProp {
  // isOpen: boolean
  // setIsOpen: Dispatch<SetStateAction<boolean>>
  children: ReactNode
  id: number
}
